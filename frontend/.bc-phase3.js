export const meta = {
  name: 'blackcloud-app-shells',
  description: 'Build 9 immersive BlackCloud app-shell pages (console) in parallel with mock data',
  phases: [{ title: 'Build', detail: 'one agent per app-shell page' }],
}

const BRIEF = 'MANDATORY: first read `docs/superpowers/specs/AGENT_BRIEF.md` (repo root /home/akash-singh/Black-Cloud-2) and follow every convention exactly. You build ONE page inside `frontend/`. Create ONLY your assigned route folder (+ any scene/subcomponents inside it); never edit shared files. These are AUTHENTICATED APP-SHELL pages: wrap page content in `AppFrame` from `@/components/layout/app-frame` with a `title` prop (it provides the sidebar, topbar, aurora) — do NOT add Navbar or SiteFooter. EXCEPTION: /login and /signup are full-screen auth pages, NOT AppFrame — they get their own bespoke centered clay auth card over a scene. All data is MOCK from `@/lib/mock` and `@/lib/catalog/nodes` (CATALOG); no backend/network. Claymorphism on every surface. Heavy but purposeful motion, reduced-motion gated. Provider/cloud icons via `@/lib/brand-icons` (ServiceIcon/PROVIDER_COLOR/TECH_ICON), UI glyphs via lucide. No hydration-unsafe time/random in render (seed from constants/index). Must pass `cd frontend && bunx tsc --noEmit` and `bun run lint` clean — run them yourself and FIX before finishing. Report files created.'

const PAGES = [
  { label: 'login', prompt: `Build /login (frontend/app/login/page.tsx + scene.tsx). Full-screen (NOT AppFrame): a centered clay auth card over a bespoke SceneShell scene of drifting particles/field. Card: BlackCloud logo, "Welcome back" heading, email + password inputs (ui/input), a primary gradient sign-in button, "Continue with Google" outline button (use a lucide icon), forgot-password + link to /signup. Use react-hook-form + zod; submit is mock → sonner toast + redirect illusion (no network). "use client". Beautiful, cinematic, claymorphism.` },
  { label: 'signup', prompt: `Build /signup (frontend/app/signup/page.tsx + scene.tsx). Full-screen (NOT AppFrame): centered clay card over a bespoke SceneShell scene (different from login — e.g. blooming constellation). Fields: name, email, password, with zod validation + inline errors; primary gradient "Create account" + Google outline; link to /login; a small trust row (SOC2, encrypted). Mock submit → sonner success. "use client".` },
  { label: 'dashboard', prompt: `Build /dashboard (frontend/app/dashboard/page.tsx). Use AppFrame title="Dashboard". Content: a row of STAT cards (from lib/mock STATS) using number-ticker (components/effects/number-ticker) in clay panels with delta chips (green/red); a projects grid from lib/mock PROJECTS as clay-pressable cards (name, provider badge via ServiceIcon/Badge, node/edge counts, status dot, cost, updated) linking to /playground; a recent-activity feed from lib/mock ACTIVITY with lucide icons per kind; a "New project" primary button. Motion stagger on mount, reduced-motion gated.` },
  { label: 'playground', prompt: `Build /playground (frontend/app/playground/page.tsx + supporting components in the same folder). Use AppFrame title="Playground". This is the flagship: a working React Flow canvas using @xyflow/react (installed). Build custom animated cloud-service nodes rendered with ServiceIcon (provider-colored clay node with status pulse + animated connection edges). Left: a node-library sidebar listing CATALOG services grouped by provider (draggable or click-to-add). Center: the React Flow canvas with Background grid, MiniMap, Controls, a few seed nodes+edges. Right: an inspector panel showing the selected node (name, provider, blurb, connections). Add a mock validation toast when an invalid edge is made (e.g. connecting two DBs) via sonner. Import "@xyflow/react/dist/style.css". "use client". Keep it performant and gorgeous with claymorphism chrome around the canvas.` },
  { label: 'ai-architect', prompt: `Build /ai-architect (frontend/app/ai-architect/page.tsx). Use AppFrame title="AI Architect". A prompt-to-architecture console: a big clay-inset prompt textarea (ui/textarea) with an example prompt prefilled, a gradient "Generate" button; on click show a mock "generating…" animated state (motion, ~1.5s using a state machine, NOT real network) then reveal results in tabs (ui/tabs): Diagram (a stylized node graph from CATALOG), Cost projection (number-ticker line items), Terraform (JetBrains-mono code block in clay-inset), Security review (checklist with status colors). Confidence indicator (violet --ai). "use client". Reduced-motion: skip the generating delay.` },
  { label: 'migration', prompt: `Build /migration (frontend/app/migration/page.tsx). Use AppFrame title="Migration". Source→target provider selectors (two clay dropdowns/segmented controls: AWS/Azure/GCP), then a visual morph panel using MIGRATION_MAP from lib/mock showing each service morphing from source to target with ServiceIcon before→after and a risk badge (low/med/high status colors). An insights row (number-ticker clay cards: complexity score, risk score, timeline weeks, est. cost). A "Start migration" primary button → sonner toast. Animated, claymorphism. "use client".` },
  { label: 'simulator', prompt: `Build /simulator (frontend/app/simulator/page.tsx). Use AppFrame title="Simulator". Left: a failure control panel — buttons to trigger AZ failure / region failure / DB failure / LB failure / service crash. Center: an architecture view (a grid/graph of clay service nodes from CATALOG) that reacts when a failure is triggered: affected nodes turn status-danger and dim, animated traffic packets (motion) reroute to survivors, a warnings feed streams messages. A "reset" button restores health. Use a small state machine for the sim; reduced-motion shows the end state without packet animation. "use client".` },
  { label: 'time-machine', prompt: `Build /time-machine (frontend/app/time-machine/page.tsx). Use AppFrame title="Time Machine". A snapshot timeline scrubber using SNAPSHOTS from lib/mock: a horizontal timeline with draggable/clickable points (motion), selecting two snapshots shows a visual DIFF panel (added nodes in green, removed in red, unchanged neutral) plus the snapshot note and node count. A play/rewind control that animates through snapshots. Claymorphism panels. "use client".` },
]

phase('Build')
const results = await parallel(PAGES.map((p) => () =>
  agent(`${BRIEF}\n\n=== YOUR PAGE ===\n${p.prompt}`, { label: p.label, phase: 'Build' })
))

return PAGES.map((p, i) => ({ page: p.label, report: results[i] ? String(results[i]).slice(0, 400) : 'FAILED/null' }))
